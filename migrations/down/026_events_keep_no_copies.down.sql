-- Nothing to undo: the payloads this removed cannot be reconstructed, and
-- putting the column back the way it was would not bring them back. The change
-- that matters is in the code that writes the row.
SELECT 1;
